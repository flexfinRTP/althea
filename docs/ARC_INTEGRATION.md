# Arc integration

## Testnet

```text
Chain ID: 5042002
RPC: https://rpc.testnet.arc.io
Explorer: https://testnet.arcscan.app
USDC ERC-20: 0x3600000000000000000000000000000000000000
```

USDC is 6 decimals on the ERC-20 interface. Native gas USDC is 18 decimals and is not used as the grant token.

## Contract

`AltheaReliefPool` on Arc. Authorized executor is the Circle agent wallet.

Settlement destination label:

```text
Example Medical Center Demo Settlement Account
```

## Mainnet

Keep `ARC_MAINNET_RPC_URL` / `ARC_MAINNET_CHAIN_ID` separate. Do not put real patient funds at risk for a prize deadline.

ETHOnline 2026 Arc Classic/From-Scratch awards may require the same project on Arc Mainnet by September 30, 2026.

## Failure copy

```text
Settlement submitted.
Waiting for confirmation.
```

Include the transaction hash when one exists. Do not invent a hash.
